name: Build and Deploy JupyterLite and JupyterBook

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install build dependencies
        run: python -m pip install -r requirements.txt
          python -m pip install 'jupyter-book==1.0.4.post1'
      - name: Build JupyterLite
        run: jupyter lite build --contents content --output-dir dist
      - name: Build JupyterBook
        shell: bash
        run: |
          set -o pipefail
          jupyter-book --version
          jupyter-book build . --builder html 2>&1 | tee jupyterbook-build.log
          mkdir -p dist/book
          cp -R _build/html/. dist/book/
      - name: Upload JupyterBook diagnostic log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jupyterbook-build-log
          path: jupyterbook-build.log
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    needs: build
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
