# Stream Network Analysis

Stream extraction, links, identifiers, and hierarchical ordering.

This chapter provides the conceptual workflow and dataset context.

Use the [interactive JupyterLite notebook](../lab/index.html?path=08_Stream_Network_Analysis.ipynb) to inspect each WhiteboxTools wrapper and run browser-safe cells.

## Dataset context

See [Open datasets and execution notes](datasets.md) for Meuse, SIDS, USGS 3DEP DEM, OpenTopography LiDAR, and hydrography sources.

## Tool: extract_streams

The interactive notebook contains the API signature, documentation lookup, dataset mapping, and guarded native Python template for extract_streams.

## Tool: stream_link_class

The interactive notebook contains the API signature, documentation lookup, dataset mapping, and guarded native Python template for stream_link_class.

## Tool: stream_order

The interactive notebook contains the API signature, documentation lookup, dataset mapping, and guarded native Python template for stream_order.

## Tool: strahler_stream_order

The interactive notebook contains the API signature, documentation lookup, dataset mapping, and guarded native Python template for strahler_stream_order.

## Tool: stream_link_identifier

The interactive notebook contains the API signature, documentation lookup, dataset mapping, and guarded native Python template for stream_link_identifier.
